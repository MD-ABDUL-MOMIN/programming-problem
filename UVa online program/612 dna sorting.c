#include<stdio.h>
#include<string.h>
int main()
{
    int i,j,m,n,N;
    scanf("%d",&N);
    while(N>=1)
    {
        scanf("%d%d",&n,&m);
        char a[n];
        for(i=0;i<n;i++)
            gets(a[i]);

    }
    return 0;
}
